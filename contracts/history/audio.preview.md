# audio.preview

Version 1.2.0: ephemeral, immutable PCM snapshots supplied by the governed desktop capture owner to the real STT provider for replaceable `transcript.partial` hypotheses. Preview requests are coalesced, never persisted, and never produce committed words or final transcript history.
