# `ui.command` 1.0.0

Initial loopback UI command envelope. It limits the browser to governed edits, session lifecycle requests, copy requests, and session-identity folder requests; it contains no arbitrary path or service-internal field.
