# audio.flush

Version 1.3.0: the governed desktop capture owner may include the stable active `utterance_id` so a replaceable preview and its authoritative final output share identity. The existing finalization semantics remain unchanged.

The governed rollover boundary uses `reason: "size"` so automatic audio-window finalization retains its authoritative boundary reason.

Version 1.0.0: the governed desktop capture owner asks the real STT provider to finalize already accepted audio before Stop or Close. The provider may emit only output derived from its actual retained audio window.
