{
  "service_name": "whisper-cpp-stt",
  "version": "1.0.0",
  "runtime": {
    "kind": "node",
    "entrypoint": "index.mjs",
    "environment": {
      "allow": ["ARGUS_SESSION_ROOT", "ARGUS_WHISPER_BINARY", "ARGUS_WHISPER_MODEL"]
    }
  },
  "permissions": {
    "filesystem": { "read": ["session-root"], "write": ["session-root"] },
    "process": { "granted": true }
  },
  "ports": {
    "domain": {
      "accepts": ["audio.chunk", "audio.flush"],
      "emits": ["transcript.partial", "transcript.word-committed", "transcript.utterance-boundary"]
    },
    "control": {
      "accepts": ["lifecycle.health-check", "lifecycle.drain"],
      "emits": ["service.failure", "service.health", "service.drained", "operation.completed"]
    }
  },
  "state": "bounded in-memory live PCM window; raw audio is temporary and deleted after each Whisper invocation",
  "side_effects": ["child-process:whisper.cpp", "filesystem:temporary-session-scoped-wav-and-json"]
}
